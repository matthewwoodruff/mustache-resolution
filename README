# Mustache Resolution

Test the app by running
`./gradlew fatJar && java -jar build/libs/mustache-resolution-all.jar`

Using mustache java 0.8.18 works although 0.9.xx is not able to resolve partial imports
